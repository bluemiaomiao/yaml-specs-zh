```yaml
%YAML 1.2
---
YAML: YAML不是标记语言™

YAML是什么:
  YAML是一种适用于所有编程语言的人性化数据序列化语言。

YAML资源:
  YAML规范:
  - YAML 1.2:
    - 1.2.2 修订版      # 2021年10月01日 *新*
    - 1.2.1 修订版      # 2009年10月01日
    - 1.2.0 修订版      # 2009年07月21日
  - YAML 1.1
  - YAML 1.0

  YAML Matrix Chat:  '#chat:yaml.io'         # 我们新的群组聊天室！
  YAML IRC 频道:      libera.chat#yaml       # 旧的聊天
  YAML 新闻:          twitter.com/yamlnews
  YAML 邮件列表:      yaml-core              # 过时的，但历史悠久

  GitHub上的YAML:                            # github.com/yaml/
    YAML 规范:        yaml-spec/
    YAML 1.2 语法:    yaml-grammar/
    YAML 测试套件:    yaml-test-suite/
    YAML 议题:        issues/

  YAML引用解析器:
  - 生成的引用解析器
  - YPaste 交互式解析器

  YAML测试矩阵:   matrix.yaml.io

YAML框架和工具:
  C/C++:
  - libfyaml      # "C" YAML 1.2 处理器 (YTS)
  - libyaml       # "C" 快速YAML 1.1 (YTS)
  - libcyaml      # C数据序列化/反序列化器 (using libyaml)
  - yaml-cpp      # C++针对YAML 1.2的实现

  Crystal:
  - YAML          # 来自标准库的YAML 1.1

  C#/.NET:
  - YamlDotNet    # YAML 1.1/(1.2) 序列化库 (YTS)
  - yaml-net      # YAML 1.1 库

  D:
  - D-YAML        # YAML 1.1 库 w/ 官方社区支持 (YTS)

  Dart:
  - yaml          # YAML的Dart包

  Delphi:
  - Neslib.Yaml   # YAML 1.1 Delphi的libyaml绑定 (YTS)

  Elixir:
  - yaml-elixir   # YAML的Elixir语言支持

  Erlang:
  - yamerl        # YAML的Erlang语言支持

  Golang:
  - Go-yaml       # YAML的Go语言支持
  - Go-gypsy      # 用Go实现的简易YAML解析器
  - goccy/go-yaml # YAML 1.2 的纯Go实现

  Haskell:
  - HsYAML         # YAML 1.2 的纯Haskell实现 (YTS)
  - YamlReference  # Haskell 1.2 引用解析器
  - yaml           # YAML 1.1 Haskell框架 (基于libyaml)

  Java:
  - SnakeYAML Engine  # Java 8+ / YAML 1.2
  - SnakeYAML         # Java 5 / YAML 1.1
  - YamlBeans         # JavaBean与YAML之间的转换. YAML 1.0/1.1
  - eo-yaml           # YAML 1.2 for Java 8. 模块化包 (Java 9+)
  - Chronicle-Wire    # 针对Java语言的YAML实现

  JavaScript:
  - yaml          # JavaScript解析器/字符串生成器 (YAML 1.2, 1.1) (YTS)
  - js-yaml       # 由原生 PyYAML 移植到 JavaScript  (演示版本)

  Nim:
  - NimYAML       # YAML 1.2 的纯Nim实现 (YTS)

  OCaml:
  - ocaml-yaml    # YAML 1.1/1.2 通过 libyaml 绑定
  - ocaml-syck    # YAML 1.0 通过syck绑定

  Perl模块:
  - YAML          # 原生 YAML 1.0 模块
  - YAML::XS      # libyaml的绑定
  - YAML::Syck    # libsyck的绑定
  - YAML::Tiny    # 一个小型的YAML子模块
  - YAML::PP      # 一个 YAML 1.2/1.1 处理器 (YTS)

  PHP:
  - The Yaml Component  # Symfony Yaml Component (YAML 1.2)
  - php-yaml            # libyaml的绑定 (YAML 1.1)
  - syck                # syck的绑定 (YAML 1.0)
  - spyc                # yaml 加载器/ (YAML 1.?)

  Python:
  - PyYAML        # YAML 1.1, 纯Python的 libyaml 绑定
  - ruamel.yaml   # YAML 1.2, PyYAML的更新; 来自评论
  - PySyck        # YAML 1.0, syck的绑定
  - strictyaml    # 受限的YAML子集

  R:
  - R YAML        # libyaml的R包装

  Raku:
  - YAMLish       # 移植YAMLish 到 Raku
  - YAML::Parser::LibYAML  # LibYAML的包装

  Ruby:
  - psych         # libyaml的包装 (在Ruby1.9.2中)
  - RbYaml        # YAML 1.1 (PyYAML的移植)
  - yaml4r        # YAML 1.0, 标准库syck的绑定

  Rust:
  - yaml-rust     # YAML 1.2 纯Rust的实现
  - serde-yaml    # struct的Rust序列化和反序列化的实现

  Shell:
  - parse_yaml    # 使用sed和awk的bash简单实现
  - shyaml        # jq的方式读取YAML文件

  Swift:
  - Yams          # libyaml的包装

  Others:
  - yamlvim       # 用原生vimscript实现的dumper/emitter

相关项目:
  - Rx            # JSON和YAML的多语言模式工具
  - Kwalify       # JSON/YAML的Ruby语言模式工具
  - pyKwalify     # JSON/YAML的Python语言模式工具
  - yatools.net   # Visual Studio的YAML编辑器
  - JSON          # JSON的官方网站
  - Pygments      # Python语言语法着色程序 /w YAML 支持
  - yamllint      # 基于PyYAML的YAML Linter
  - YAML Diff     # 在语义上比较两个YAML文档
  - JSON Schema   # 用于数据校验，符合YAML的JSON标准

```
